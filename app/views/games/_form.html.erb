
<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <div class="input-field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

    <!-- <div class="field input-field">
      <%= f.label :sport %><br>
      <%= f.text_field :sport %>
    </div> -->
  <div class="row">
  <div class="input-field col s6">
    <div class="field input-field">
      <%= f.label :date %><br>
      <%= f.date_field :date %>
    </div>
  </div>

  <div class="input-field col s6">
    <div class="field input-field">
      <%= f.label :time %><br>
      <%= f.time_field :time %>
    </div>
  </div>
</div>

    <div class="field input-field">
      <%= f.label :location %><br>
      <%= f.text_field :location, class:"address" %>
    </div>

    <!-- <div class="field input-field">
      <%= f.label :dynamic %><br>
      <%= f.text_field :dynamic %>
    </div> -->

  <div class="row">
  <div class="input-field col s4">
    <div class="field input-field">
      <%= f.label :price %><br>
      <%= f.number_field :price, in: 0.0..20.0, step: 5 %>
    </div>
  </div>
  </div>

    <br>
    <div class="actions">
      <%= f.submit class: "btn waves-effect waves-light btn-large" %>
    </div>
    <br>
    <br>

  
<% end %>

